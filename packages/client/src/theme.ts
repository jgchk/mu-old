export interface Theme {
  fonts: {
    display: string
    body: string
  }
}

const theme: Theme = {
  fonts: {
    display: "'Montserrat', sans-serif",
    body: "'Roboto', sans-serif",
  },
}

export default theme
